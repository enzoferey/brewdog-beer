import React from "react";
import PropTypes from "prop-types";
import style from "./button.scss";

const Button = () => (
	<div className={style.main}>Button</div>
);

Button.propTypes = {
	
}

export default Button;